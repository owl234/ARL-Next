# 🤖 AI Agent 核心准则

> 处理本项目时，必须**严格遵守**以下规范。

## 1. 核心思维与行为红线
- **第一性原理与深度思考**: 面对复杂需求或 Bug，禁止生搬硬套过往经验。必须剥离表象，回归基础事实进行逻辑推导，**放慢分析步伐，禁止跳跃式决策**。
- **无痕执行与安全红线**:
  - 主动创建的临时验证文件必须在任务结束后清理。
  - 涉及销毁容器、清理网络、丢弃未追踪文件（如 `git reset --hard` / `git clean -fd`）等破坏性操作前，**必须向用户说明影响并获批**。
  - **版本控制红线**：严禁擅自 `git commit` 或破坏性回退；日常严禁修改 `version.txt` 与 `CHANGELOG.md`。仅在用户输入 "WIP" 时触发全局快照，Push 前协助 Squash 合并。
  - **数据安全与向下兼容红线**：
    - **严禁破坏性卷变更**：严禁擅自修改 `docker-compose.prod.yml` 中的持久化数据卷（Volume）名称（如 `mongo_data`）或挂载路径。若涉及重大架构调整，必须在部署与更新链路中内置全自动无损平滑迁移逻辑，严禁直接覆盖导致新卷挂载丢失用户历史数据。
    - **数据库平滑演进与索引防崩**：严禁破坏性变更存量 MongoDB 字段结构；新增字段或变更默认值时，必须在 `backend/app/utils/arlupdate.py` 中补充平滑兼容与自动补齐逻辑；新增联合唯一索引或约束时，必须在 `arlupdate.py` 中内置基于聚合管道的自动去重与容错降级机制，彻底杜绝 E11000 重复键冲突引发的服务启动崩溃。

## 2. 编码与架构规范
- **代码整洁与极简**: 坚持“少即是多”。避免过度设计，UI 最小化层级嵌套；函数只做一件事，优先保证人类可读性（禁止取巧写法）；优先使用标准库（最少依赖），并必须显式捕获与处理异常。

## 3. CI/CD 与部署闭环
- **Docker 优化**: 修改 `Dockerfile.prod` 必须强制使用**多阶段构建 (Multi-stage Build)**，彻底隔离编译工具与纯净运行时，极限压缩镜像体积。
- **热更新防污染机制**: 打包本地代码进行热更新（如 `docker cp`）时，**必须强制排除跨平台虚拟环境目录**（如 `.venv-docker`, `.tmp_venv`, `__pycache__` 等）。严禁将 Mac/Windows 本地的环境包覆盖至远端 Linux 容器，否则会导致 Python 环境彻底瘫痪且无法自愈。
- **配置与性能**: 版本信息存放于 `version.txt`，服务器 SSH 登录连接信息存放于 `server.txt`。更新 Nginx 配置时必须开启 Gzip 防止明文传输大文件。
- **正式发版流程 (仅在用户明确发出“发版/Release”指令时触发)**:
  - **严禁提前发版**：在用户明确发出发版指令前，所有日常工作均属于开发/调试态，绝对禁止自动修改版本号与更新日志，禁止自动执行正式发版提交。
  - **发版四步法**：
    1. **触发与全量审查**：收到用户明确发版指令后，通过 `git diff` 导出全量差异进行深度审阅，提炼底层重构与全部变更；**必须执行存量数据兼容性审查**，严格排查 `docker-compose.prod.yml` 卷配置、MongoDB 集合与字段读写变更、以及 `arlupdate.py` 增量迁移逻辑，确保从上一版本升级而来的老用户数据 100% 完整保留且无感平滑升级。
    2. **同步版本与日志**：同步更新 `version.txt`（递增版本号）与 `CHANGELOG.md`（按规范分类对齐完整特性），协助用户 Squash WIP 提交，并由用户确认后生成正式发版 Commit。
    3. **CI 校验与 Tag 确认**：检查 `Dockerfile.prod` 规范，并确认 `CHANGELOG.md` 包含当前 Tag 章节；Push 到 `origin` 前询问是否需打 Tag (`v*`) 触发自动构建与 GitHub Release。
    4. **CD 部署**：本地 `docker-compose.yml` 强制使用阿里云 ACR 前缀拉取内网镜像，严禁使用官方裸镜像（如 `mongo:7.0`）。执行 `start-prod.sh` 前必须确认最新镜像已就绪。

## 4. 文档与输出规范
- **全局语言**: 生成的内容与 Artifacts 必须使用**中文**。
- **代码审查与扫描 (Review & Diff)**: 审查严禁仅看局部，必须完整追溯调用链上下文；提炼变更特性时**绝对禁止**仅看 `git log/status`，必须通过导出完整差异（如 `git diff -U0`）深度审阅，挖掘底层重构，确保 100% 无死角不遗漏。
- **更新日志规范 (CHANGELOG.md)**: 作为全项目唯一真实更新源 (Source of Truth)，必须采用结构化分类（🚀 新增功能 / 🛠️ 性能与重构 / 🐛 问题修复与加固 / 🛡️ 安全与配置），严格与代码差异深度对齐，杜绝漏项。
- **Git Commit**: 必须采用**严谨、客观、专业**的工程化表述（如“规避”、“收敛”）。严禁主观营销与情感词汇（如“完美解决”、“彻底消除”），精确传达技术价值。
- **微信发版通知**: 必须遵守 `Notify.txt` 规范。**极致精简、对仗工整、口语化**，基于 `CHANGELOG.md` 将底层技术（如 bulk_write）转化为群友可感知的体验升级（如“拒绝卡死”），严禁废话。

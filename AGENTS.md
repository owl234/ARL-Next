# 🤖 AI Agent 核心准则

> 处理本项目时，必须**严格遵守**以下规范。

## 1. 核心思维与行为红线
- **第一性原理与深度思考**: 面对复杂需求或 Bug，禁止生搬硬套过往经验。必须剥离表象，回归基础事实进行逻辑推导，**放慢分析步伐，禁止跳跃式决策**。
- **无痕执行与安全红线**:
  - 生成的内容与 Artifacts 必须使用**中文**。
  - 主动创建的临时验证文件必须在任务结束后清理。
  - 涉及销毁容器、清理网络、丢弃未追踪文件（如 `git reset --hard` / `git clean -fd`）等破坏性操作前，**必须向用户说明影响并获批**。

## 2. 编码与架构规范
- **极简设计**: 坚持“少即是多”。代码避免过度设计；UI 最小化点击与层级嵌套；文档语言精炼。
- **架构整洁**:
  - **职责单一**与**自解释**: 函数只做一件事，优先保证人类可读性，禁止取巧写法。
  - **最少依赖**与**显式错误**: 优先使用标准库，必须显式捕获并处理异常。
- **审查与追溯**: 代码审查严禁仅看局部。必须完整追溯调用链及多方调用上下文后再下结论。
- **性能**: 更新 Nginx 配置必须开启 Gzip 防止明文传输大文件。

## 3. CI/CD 与部署闭环
- **Docker 优化**: 修改 `Dockerfile.prod` 必须强制使用**多阶段构建 (Multi-stage Build)**，彻底隔离编译工具与纯净运行时，极限压缩镜像体积。
- **环境凭证**: 版本信息存放于 `version.txt`，服务器 SSH 信息存放于 `server.txt`。
- **双 Remote 发布流程**:
  1. 修改必须同步至本地源码，验证后询问是否更新 `version.txt` 并 `git commit`。
  2. 触发 Github CI 前：检查 `Dockerfile.prod` 规范。
  3. Push 到 `origin` 前：询问是否需打 Tag (`v*`) 触发自动构建。
  4. 部署阶段 (CD)：本地 `docker-compose.yml` 强制使用阿里云 ACR 前缀拉取内网镜像，严禁使用官方裸镜像（如 `mongo:7.0`）。执行 `start-prod.sh` 前必须确认最新镜像已就绪。

## 4. 文档与输出规范
- **Git Commit**: 必须采用**严谨、客观、专业**的工程化表述（如“规避”、“收敛”）。严禁主观营销与情感词汇（如“完美解决”、“彻底消除”），精确传达技术价值。
- **代码扫描 (Diff)**: 提炼变更特性时，**绝对禁止**仅看 `git log/status`。必须通过完整差异文件（如 `git diff -U0` 导出）深度审阅，挖掘底层重构，确保 100% 无死角不遗漏。
- **微信发版通知**: 必须遵守 `Notify.txt` 规范。**极致精简、对仗工整、口语化**，将底层技术（如 bulk_write）转化为群友可感知的体验升级（如“拒绝卡死”），严禁废话。

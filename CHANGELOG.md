# 更新日志 (Changelog)

本文档记录 ARL-Next 项目的所有重大版本演进、新特性、性能优化与漏洞修复。

---

## [v1.3.4] - 2026-09-12

### 🚀 新增功能
- **全链路资产画像拓扑聚合与可视化下钻**：资产组详情页新增「全链路画像」专属板块，基于目标子域名与 IP 实现双向全链路资产拓扑并行聚合（子域名 ➔ 解析 IP ➔ 关联 C 段 ➔ 开放服务 ➔ Web 站点与截图 ➔ SSL 证书 ➔ 敏感文件泄露 ➔ 敏感 URL 与 WIH ➔ 风险漏洞与 Nuclei）；集成 7 大维度折叠面板、自适应域名联想搜索、一键展开折叠与跨 Tab 画像联动，并升级为 Blob 流式对象导出，杜绝大数据截断。
- **ip2region 与 GeoLite2 双轨高精地理定位融合**：引入纯 Python 离线 `xdb_searcher` 引擎并内置高精度 IP 库（`ip2region_v4.xdb`），单次检索微秒级响应；实现双轨融合决策，国内 IP 优先采用高精省市与 ISP 运营商，海外 IP 自动回退 MaxMind，配合 `geo_pinyin.py` 最长前缀倒序匹配与港澳台运营商归一化，提供省市区县级精准归属。
- **MCP 协议标准库零依赖重构与全链路低耗收敛**：`mcp-server` 全面重构为基于 Python 3.8+ 标准库的标准 JSON-RPC 2.0 Stdio 服务端，剥离全部三方依赖，实现秒启（<20ms）；收敛为单核心 `get_asset_profile` 工具，后端新增 `/asset_profile` 统一画像接口并引入 `_clean_empty` 递归剪枝器，压缩单资产 80% 以上 Token 开销并建立防爆截断与双轨容灾机制。
- **前端多 Tab 独立状态持久化与分层缓存治理**：封装 `useTabStateCache.js`，实现资产分组详情页内 10+ 独立 Tab 的滚动位置、分页页码、检索条件及当前激活 Tab 的独立持久化；支持多 Tab 跨板块穿透下钻与深层跳转参数收敛。
- **Worker 运行态并发感知与热扩缩容状态联动**：系统管理中集成 Celery 运行态动态并发感知，支持实时读取各 Worker 节点的实时并发数与负载状态，实现热扩缩容状态联动与优雅缩容调度，控制台提供直观状态反馈与二次确认机制。

### 🛠️ 性能与重构
- **资产服务端点多项折叠与弹窗分流**：针对海量开放端口资产，抽取 `ServiceDetailModal.vue` 弹窗分流组件，主表格收敛展示前 3 项端点，弹窗内提供独立检索、分页与一键快速复制，消除超长单元格对页面布局的撑爆破坏。
- **前端 GPU 离屏渲染优化与内存防雪崩**：彻底移除大表格单元格内深度递归的 `backdrop-filter` 高斯模糊滤镜，消除大数据量场景下的 GPU 离屏渲染掉帧瓶颈；将图片 Base64 编码机制升级为 `URL.createObjectURL` 临时对象，释放前端堆内存。
- **监控调度全链路溯源增强与搜索引擎挖掘**：`DomainExecutor.run` 在资产周期监控场景下补齐 `search_engines`（必应与 360）页面挖掘能力；预载存量资产真实发现渠道至映射表，保障周期扫描感知变动时技术来源真实留痕。
- **指纹求值引擎校准与 AC 自动机短路判定**：修正 `expr.py` 正则求值入参顺序倒置问题，对齐 AST 求值约定；引入伪正则自动提升与 `fingerprint_cache` 大小写预处理短路判断，并增量同步 `Byte-nginx`、`Nginx`、`ByteDance-TLB` 核心加固规则。

### 🐛 问题修复与加固
- **资产库同步 C 段网段合并覆盖缺陷修复**：修复 `syncAsset.py` 中 `asset_cip` 采用 `ReplaceOne` 暴力覆盖导致多主域名任务间 C 段相互冲刷的问题，改为 `ip_list` 与 `domain_list` 并集去重合并；`arlupdate.py` 新增 `migrate_asset_cip_merge` 迁移步骤自动修复存量冲刷数据。
- **资产监控技术溯源防降级保护**：在资产同步入库拦截逻辑中增加校验，若存量资产已有具体技术发现途径（字典爆破、证书提取、搜索引擎等），严禁被 `monitor` 周期监控弱标记降级覆盖。
- **SSL 证书导出功能对称性加固**：在 `routes/cert.py` 中补齐 `/export/` 导出接口实现，修复证书导出中端口空值回退（`port = item.get('port') or 443`）；在 `assetCert` 中补齐 `cert.extensions.subjectAltName` 检索映射。
- **站点资产检索收敛与幽灵过滤器拦截**：收敛站点检索无效冗余参数，增加参数白名单拦截幽灵字段；`App.vue` 引入 `reset_bg=1` 应急逃生机制联动 `dbHelper.clear()` 彻底清空前端脏缓存。

### 🛡️ 安全与配置
- **资产分组站点「待测试」标签全生命周期治理与防误复活**：存量资产站点日常监控扫描不再盲目复活「待测试」标签，仅在探测到新指纹、标题变更或 HTTP 状态码变动时触发打标；`arlupdate.py` 引入哨兵幂等自愈，单次平滑补齐存量数据并展平历史嵌套标签。
- **存量地理信息后台异步平滑自愈**：`arlupdate.py` 增设 `migrate_geo_ip_data` 后台守护线程，引入 `system_config` 全局哨兵标记与批量写入机制（每批 1000 条），在不阻塞系统启动锁前提下静默补齐存量 IP 资产的高精度地理数据。
- **MCP 服务端脱敏与配置防护**：全面脱敏 MCP 协议配置文件与使用文档中的真实测试公网 IP，收敛外网暴露风险；补齐 JSON-RPC 2.0 异常处理与连接断开自愈。

---

## [v1.3.3] - 2026-09-09

### 🚀 新增功能
- **工信部 ICP 移动端资产侦察与域名智能识别**：新建 ICP 任务表单引入企业名称与域名目标智能校验，当选择移动端（APP/小程序/快应用）查询却误填域名时实时预警并防御阻断，引导输入企业全称；资产详情页重构定制移动端专属资产列（`mobileIcpColumns`），全面展示应用名称、服务备案号、主办单位名称、主备案号、前置审批类型及审核日期，并兼容历史新旧接口字段；Excel 导出重构为通用抽象组件，支持各类资产一键流式导出。
- **PoC 弱口令插件标准模板与导出支持**：Web 源码编辑器新增独立 Python 弱口令爆破模板（`python_brute`），对齐 `check_app` 指纹预检与 `login` 验证规范契约，并显式提示内置字典依赖；重构模板下载逻辑，支持一键下载标准 Python PoC 插件代码框架。
- **WIH 敏感信息多维精确与排除筛选**：全局资产检索、资产分组详情与任务详情内的 WIH 模块全面对齐操作符映射，`record_type` 支持“等于”精确匹配与“不等于”排除匹配；后端新增 `record_type__eq` 语法糖，完善数据检索体验。

### 🛠️ 性能与重构
- **ICP 资产并发批量落盘与索引防超限优化**：重构 OSINT 任务入库链路，由逐条插入全面升级为 `insert_many(..., ordered=False)` 批量并行写入；`arlupdate.py` 增设 `icp_asset` 核心复合索引（`task_id` + `query_type` + `updateRecordTime/examineDate`），从根本上消除 COLLSCAN 全表扫描与 MongoDB 32MB 内存排序溢出崩溃。
- **系统增量迁移单步容错兜底机制 (`_run_step`)**：`arlupdate.py` 核心迁移流程引入单步容错隔离机制，内置字典检查、指纹增量同步、索引构建、PoC 更新及僵尸任务清理等单项异常均被独立隔离且全面留痕，彻底杜绝因个别插件或脏数据导致整机启动更新流程被阻断的问题。
- **生产部署端口转发全自动自愈与守护健康校验**：`start-prod.sh` 引入 `verify_and_heal_port_forwarding` 自愈探针，部署完毕后通过真实 TLS 握手连续探测 5173 前端网关，遇到因系统防火墙操作冲掉内核 NAT 转发链时全自动触发 Docker 联动重载恢复；系统更新服务（`arl-updater`）新增 8888 端口防冲突安全清理与 `systemctl is-active` 严格健康探测。

### 🐛 问题修复与加固
- **工信部 ICP 任务状态解耦与手动中止即时响应**：修复 ICP 深度查询中部分非关键接口受挫导致整个任务直接置为 `error` 且阻断资产同步的问题——优化为只要获取到资产即判定为 `done` 并畅通一键同步资产组；修复用户手动点击停止任务后后台仍在继续发包的问题，在每类查询循环前增加中止检测与安全退出。
- **PoC 插件解析与同步纵深防御**：`npoc.py` 解析插件列表时对 `scheme` 字段引入多类型兼容（list/tuple/set/str）与异常清洗，规避外部第三方非法插件因类型不合规引发 TypeError 导致服务启动或插件库同步整体中断；完善单个毒插件跳过与报错留痕机制。
- **部署脚本 Firewalld 联动重载与 CentOS 7 源兼容**：修复 CentOS / RHEL 环境下防火墙规则调整导致 Docker 容器网络瘫痪的问题，实现 `firewall-cmd --reload` 与 Docker 内核转发链联动重载；兼容 CentOS 7 官方归档源与 Docker CE 源稳定安装。

### 🛡️ 安全与配置
- **PoC 源码 AST 静态语法合规校验与沙箱防御**：新建与编辑 PoC 插件时，后端引入基于 Python AST 抽象语法树的多层静态校验，拦截 `exec`、`eval`、`subprocess`、`os.system` 等高危动态调用，强制要求继承 `BasePlugin`，校验必填属性与规范命名；强制限制仅允许新建 `.py` 格式插件。
- **全插件目录防重名冲突归类**：新建插件时自动遍历 `system_plugins` 全目录（poc、brute、sniffer、pocs）防重名遮蔽冲突；根据源码内容智能归类存放路径。
- **工信部查询反爬熔断与 Token 异步双检锁续期**：`ymicp.py` 引入创宇盾 WAF 熔断机制与并发安全标志位，一旦遭遇反爬拦截立即平滑降级并终止后续请求，避免无效轰炸导致 IP 被封禁；引入 Token 异步双检锁（DCL），并发获取详情时仅由首个协程安全换取新 Token 并保持会话凭据连续。

---

## [v1.3.2] - 2026-09-07

### 🚀 新增功能
- **正式确立 GNU GPL-3.0 开源协议与版权声明**：根目录正式引入 `LICENSE`（GNU General Public License v3.0）并完善 README 协议条款与权利义务说明，明确社区自由使用、修改定制、传染同协议开源与免责声明，规范二次开发衍生作品分发，促进社区合规共建。
- **平台管理员密码强制重置机制 (`--reset`)**：`inject_user.py` 补充 `--reset` 参数控制逻辑，生产容器冷启动或常规重启时自动保护存量管理员口令不被覆盖；运维人员在遗忘密码时可通过终端显式传参（`--reset`）一键强制重置为默认口令 (`arlpass`)。

### 🛠️ 性能与重构
- **脚本执行路径与虚拟环境隔离加固**：`backend/inject_user.py` 内部引入基于 `__file__` 的绝对路径解析并注入 `sys.path`，摆脱对当前工作目录的隐式依赖；重置指令与单行直更指令全面对齐容器内专属虚拟环境解释器（`/code/backend/.venv-docker/bin/python3`），规避外部环境污染与模块加载缺失。
- **更新守护进程全架构文件平滑同步**：`updater.py` 容器文件提取更新逻辑扩充同步 `version.txt`、`CHANGELOG.md` 及 Nginx 基础配置 `frontend/default.conf.prod`，确保存量宿主机在 Web 一键升级后版本元数据与网关规则即时生效，同时物理隔离保护宿主机现存 `frontend/.htpasswd` 凭据文件。

### 🐛 问题修复与加固
- **免 Git 一键部署与手动升级指令修复**：README 中 Ubuntu/CentOS 极速连缀安装指令补充 `docker cp` 提取 `frontend` 目录，规避 Nginx 网关启动时因缺失挂载目录引发的异常；手动升级指令同步补齐 `default.conf.prod` 平滑覆盖逻辑。
- **规范化代码格式与 Whitespace 严苛清洗**：全面清理代码与文档尾部冗余空白字符与文件末尾多余空行，严格通过 `git diff --check` 格式校验。

### 🛡️ 安全与配置
- **管理员重置权限模型与盐值风险警示**：在 README 运维 FAQ 中补充 `[!CAUTION]` 安全警示，详述 `--reset` 传参与 Python 直更指令对 Docker Exec 权限的依赖性与免二次认证特性，提醒生产环境收敛宿主机 Docker 用户组权限并规避匿名 docker.sock 暴露。

---

## [v1.3.1] - 2026-09-07

### 🚀 新增功能
- **资产监控任务重新运行链路闭环**：彻底打通资产监控任务（`task_tag: monitor`）手动重启能力，精准区分周期监控任务与一次性扫描任务（Oneshot）；创新引入“预建 WAITING 任务记录 + Celery 路由调度 + Worker 动态收养”机制，支持失败自动回滚删除，后端响应并透传新任务 ID，前端支持无感原地刷新与二次点击防重放。
- **云原生服务未授权访问 PoC 插件**：ARL-NPoC 引擎扩充新增 Etcd 未授权访问插件（`Etcd_noauth.py`，支持 HTTP REST 与 gRPC-Gateway 双探针精确验证）与 Kubelet API 未授权访问插件（`Kubelet_noauth.py`，覆盖 `/pods` 与 `/runningpods/` 并采用严格 PodList/容器 Spec 特征杜绝误报）。
- **开源社区矩阵与支持作者浮窗**：前端顶部导航栏集成支持作者浮窗组件（`SupportAuthorPopover.vue`），集成微信支付/支付宝打赏、官方微信公众号（「owl安全」）与作者微信交流群，支持暗黑模式自适应与无图矢量 SVG 优雅降级。
- **宿主机物理内存可观测与运行预检**：仪表盘（Dashboard）系统监控重构，新增宿主机物理内存已用/总计/可用容量、Swap 虚拟内存使用率与总量指标，并配备 `critical` (>=90%) / `warning` (>=80%) 实时双级告警 Tag 与智能悬浮气泡；部署脚本 `start-prod.sh` 新增 `check_host_memory()` 物理内存预检探针（全栈软保留约 1.8G，低内存只告警不阻断）。

### 🛠️ 性能与重构
- **全栈解除 Docker 容器内存硬限制**：从 `docker-compose.prod.yml` 中全面移除前端、Web、Puppeteer、Worker、MongoDB、RabbitMQ、OSINT、Autoheal 等微服务的 `mem_limit` 容器硬性限额，释放宿主机多核大内存性能潜力，规避高并发大规模扫描与复杂聚合时核心容器被内核 OOM-Killed 意外中断。
- **升级 Nmap 7.95 黄金 LTS 稳定版**：多阶段构建中将 Nmap 从旧版编译升级至 7.95 黄金 LTS 稳定版，扩充 2500+ 服务指纹与 336 种现代化 OS 指纹并修复核心内存泄漏；针对 ARM64 / QEMU 交叉编译裁剪剥离 `zenmap`、`ndiff`、`nping`、`ncat` 等臃肿 C++ 模板依赖，锁定编译并发数（`max 2`），彻底规避交叉编译段错误；优化 `/usr/share/nmap` 软链接布局消除冗余镜像层。
- **系统性能默认并发数收敛**：调整 `system_config.py`、`performance_config.py` 与 `start_worker_prod.sh` 默认并发基线，将 Heavy 与 Light 队列默认并发数从 2 收敛为 1，大幅降低单核/双核小内存 VPS 首次部署的负载压力，提供更稳健的开箱体验。
- **PoC 详情抽屉交互与安全加固**：重塑 PoC 管理详情抽屉交互，危险级别采用彩色语义 Tag（Critical/High/Medium/Low），修复建议配置高亮引用区块，参考链接支持带图标的外链安全跳转（`rel="noopener noreferrer"`），空值与未定义选填字段智能折叠隐藏。

### 🐛 问题修复与加固
- **资产分组死字段清理与 CRUD 边界防御**：彻底剔除资产分组文档中历史遗留且从未被扫描/监控链路消费的 `black_scope` 与 `black_scope_array` 冗余死字段，同步收敛前后端与 MCP Server 协议入参；新增组内至少保留一个资产范围的删除防御阻断；规范 PyMongo 异常捕获；子资产级联删除统一强制转换字符串 ObjectId；单项移除时同步清理 `domain_status` 字典规避幽灵状态残留。
- **在线更新器并发锁加固与阶段态势可视化**：`updater.py` 内部引入 `progress_lock` 互斥锁保障并发线程安全，分层传输进度每 3 秒聚合摘要输出至主日志，消除长时间下载无日志的卡死焦虑；前端系统更新 Modal 重构为 3 阶段态势追踪，集成动态心跳呼吸指示灯与 Docker 分层芯片滚动显示，完善组件卸载定时器销毁机制。
- **稳定版本镜像快照全面覆盖**：部署脚本 `start-prod.sh` 镜像备份由单一 Web/Worker/Frontend 扩充至全量微服务（新增 `arl-puppeteer` 与 `osint-service`），升级前全自动打标 `backup-stable`。
- **正则转义字符规范化**：修复 Python 3.12+ 环境下 `nmap.py` 中的无效反斜杠正则转义告警（使用 raw string `r'...'`）。

### 🛡️ 安全与配置
- **存量指纹与 PoC 增量平滑同步与防复活机制**：`arlupdate.py` 重构 `fingerprint_info_update()` 与 `npoc_info_update()`，确保存量升级仅增量补齐新增指纹与插件，避免覆盖用户自定义属性；引入 `fingerprint_deleted` 集合机制，用户在 UI 主动删除的内置指纹记录删除标记，不再因系统升级或重启而强制复活（重新手动添加或全量 JSON 重置时自动清除）；建立唯一索引时遇键冲突自动通过聚合管道无损去重降级，彻底杜绝 E11000 启动崩溃。
- **Nginx Basic Auth 网关策略优化**：`default.conf.prod` 中将 Basic Auth 前置网关默认状态调整为 `auth_basic off;`，消除首次安装双重账号密码的困惑与误解；保留 Web 顶部导航栏一键热开启/关闭与修改凭据功能，对齐 README 与全链路文档。
- **云原生高频端口与特征库扩充**：`webapp.json` 针对 Kubernetes、Kubelet、etcd、Nacos 等云原生资产进行深度指纹加固，清理并归并 `etcd-io` 冗余规则；在 `port_top100.txt` 与 `port_top1000.txt` 字典中扩充 2375、2376、2379、2380、8500、8848 等云原生服务核心端口。

---

## [v1.3.0] - 2026-09-01

### 🚀 新增功能
- **纯 Python WIH 敏感信息提取引擎**：彻底剥离 Go 编译外部二进制依赖，基于纯 Python 实现高精敏感特征提取与流式分块解析器，新增三级风险评估机制（CRITICAL / MEDIUM / LOW），全面覆盖主流云厂商 AK/SK、微信凭据、JWT、私钥及敏感 API。
- **首页立体态势大屏重塑**：重构全局仪表盘为 4 行立体态势大屏，集成实时资产核心指标软胶囊、双折线监控与资产增长趋势图、Top 10 指纹分布柱状图、漏洞威胁严重度立体环形图以及最新任务实时动态审计流。
- **超大字典异步分片上传**：字典管理新增异步超大文件上传管道与流式落盘处理，支持在线状态轮询、自动去重及 7 天 TTL 自动过期机制。
- **无感剪贴板与被动 DNS 扩充**：前端集成通用剪贴板工具（`clipboard.js`），支持资产一键复制；DNS 查询模块扩充 Anubis、HackerTarget 与 Wayback 被动解析插件。

### 🛠️ 性能与重构
- **仪表盘全链路聚合加速**：后端查询全量迁移至 MongoDB 聚合管道（Aggregation Pipeline），前端集成 Vue Keep-Alive 视图保活机制与骨架屏加载，彻底杜绝全表轮询带来的性能延迟。
- **资产分组探测状态持久化**：引入 `domain_status` 探测状态与覆盖度批量聚合与持久化回写机制，消除前端分组列表渲染时的全表正则扫描；`arlupdate.py` 内置存量资产分组静默预计算补齐。
- **字典管理安全加固与进程锁**：字典读写全面引入 `fcntl.flock` 跨进程/跨线程排他锁与共享锁，内置核心字典白名单防物理删除保护，严格限制目录穿越与 symlink 符号链接逃逸。
- **子域名与 CDN 规则库扩充**：新增 `domain_top300.txt` 递归高频子域名字典（引擎层内置内存兜底）；扩充并优化 `domain_2w.txt`、`altdnsdict.txt`、`cdn_info.json` 及 `webapp.json` 特征库。

### 🐛 问题修复与加固
- **僵尸任务自动收敛与调度自愈**：启动与更新时自动探测并收敛因系统重启中断的非终态任务至 `ERROR` 状态，并自动恢复周期监控调度器。
- **任务管理状态机与容错修复**：修复任务批量停止调用链容错机制与已停止任务的删除限制；企业资产侦查支持批量重启与状态机防御加固。
- **服务执行与插件调用修复**：修复 AltDNS 跨业务连字符生成返回值缺失问题，修复 FileLeak 执行方法缺失异常。
- **天眼查投资比例严格过滤**：优化天眼查股权穿透查询最低投资比例严格过滤与层数兜底逻辑。

### 🛡️ 安全与配置
- **存量数据平滑迁移与联合索引防崩**：`arlupdate.py` 内置历史 `wih` / `asset_wih` 数据的 `fnv_hash` 自动回填，并在建立 `(site, fnv_hash)` 联合唯一索引时内置聚合去重降级机制，彻底杜绝 E11000 启动崩溃。
- **Nginx 网关与更新器全链路自愈**：重构 Nginx Basic Auth 前置网关热切换与状态探测；修复 Nginx 正则匹配劫持截图 API 导致的图片加载异常；`updater.py` 增强 Docker 守护进程探测、自愈重连与 pty slave fd 泄漏规避。
- **容器资源精细化配额与内核调优**：`docker-compose.prod.yml` 保持持久化卷 100% 兼容，为所有微服务配置精细化内存限制 (`mem_limit` / `mem_reservation`) 与 `oom_score_adj` 降级保护；部署脚本固化宿主机内核参数自动调优。

---

## [v1.2.1] - 2026-08-17

### 🐛 问题修复与加固
- **字典与特征库多容器同步修复**：在 `docker-compose.prod.yml` 中为 `arl-web` 与 `arl-worker` 补充 `arl_app_dicts` 共享持久化数据卷，解决在系统设置中上传或修改字典、CDN 配置及指纹特征后，后台扫描 Worker 因跨容器隔离无法加载字典（报错字典文件不存在）的问题。
- **CI 构建与制品兼容性加固**：在 GitHub Actions 构建流程中禁用 buildx 的 `provenance` 与 `sbom` 附加层，规避阿里云 ACR 镜像推送时的兼容性报错。
- **自动更新器 Python 兼容性优化**：优化 `updater.py` 对 Python 3.6+ 环境的兼容性，并补充 `HEAD` 探针支持，提升热更新检测稳定性。

---

## [v1.2.0] - 2026-08-16

### 🚀 新增功能
- **资产监控调度双模演进**：资产分组全面支持一键下发「一次性扫描」与「周期监控」双模式，变动资产即时入库并反馈。
- **资产穿透与情报联动**：打通任务结果与资产底层全量集合（AssetSearch），天眼查（TYC）股权穿透结果支持一键直接转入资产任务。
- **字典管理现代化重构**：升级为现代三栏式交互视图，支持在线大文本预览、按行批量检索/追加/删除与 Emoji 语义化分类。
- **AI 原生 MCP 模块重塑**：将 MCP 模块全面迁移为轻量原生 Python 架构，支持向大模型与 Agent 直出 13 维全景资产大盘。
- **多通道告警与结构化排版**：重构 5 大推送通道（钉钉、企业微信、飞书、Server酱、Telegram），支持 13 维结构化资产排版，收敛防抖失效与重复推送。

### 🛠️ 性能与重构
- **操作吸附与全局状态记忆**：全站核心数据表格与顶部/底部操作栏支持悬浮吸附；全局表格分页支持最高 500 条并记忆用户偏好，页面返回自带无感静默刷新。
- **数据批量入库与阶段容错**：优化大规模 Nuclei 扫描并发写入（bulk_write）与阶段容错隔离机制，规避主键冲突与扫描中断。

### 🐛 问题修复与加固
- **任务状态约束修复**：修复任务在停止状态下的删除限制与状态对齐，完善后台扫描异常时的资源收敛。

---

## [v1.1.9] - 2026-08-14

### 🐛 问题修复
- **部署镜像兼容性修复**：补充一键部署所需的 frontend 静态资源目录至打包镜像，规避 `start-prod.sh` 在部分纯净宿主机环境下的初始化报错。

---

## [v1.1.8] - 2026-08-12

### 🚀 新增功能
- **系统级图形化安全加固**：系统配置新增 Nginx Basic Auth 安全防护图形化热切换，自动覆写并热重载网关。
- **C 段资产拓扑透视**：新增 `CidrDetailModal` 聚合弹窗组件，支持点击 C 段直观展示网段内 IP 与域名拓扑关系。
- **HTTP 响应异常探针**：新增基于 HTTP 响应体长度（`body_length`）异动的站点监控机制，结合有限状态机防抖策略降低误报。

### 🛠️ 性能与重构
- **运行时与数据库大版本升级**：
  - 核心运行环境升级至 Python 3.13 与 Celery 最新稳定版；修复 `urllib3` 废弃 `get_host` 的兼容性问题。
  - 核心数据库升级至 MongoDB 7.0，默认配置 `--wiredTigerCacheSizeGB 1` 精准约束底层引擎内存占用；提供 `upgrade-mongo.sh` 平滑迁移脚本。
- **多阶段 Docker 构建**：重构 `Dockerfile.prod` 为 Multi-stage Build，彻底隔离编译依赖与纯净运行时，大幅压缩生产镜像体积。
- **高并发上下文与索引优化**：引入 `contextvars` 与 `ContextAwareThreadPoolExecutor` 解决异步切换时任务 ID 丢失问题；为系统日志建立 TTL 与 task_id 复合索引。
- **WIH 敏感信息聚合**：重写 `AssetWihMonitor` 为微批次聚合（Micro-batching）并发写入，规避海量指纹落库时内存挤压。

### 🐛 问题修复与安全加固
- **WIH 去重算法修正**：将指纹去重粒度修正为 `(site, fnv_hash)` 联合维度，防止同一敏感文件在不同子域下被误判覆盖。
- **外部指令防注入**：修复 `infoHunter.py` 潜在的命令注入隐患，强制引入 `shlex.quote` 参数过滤。
- **初始化锁与唯一索引兜底**：`arlupdate.py` 引入分布式锁规避多 Worker 并行启动竞争条件，并增加唯一索引冲突（E11000）兜底容错。

---

## [v1.1.7] - 2026-08-05

### 🚀 新增功能
- **资产全量接口升级**：AssetSearch 重构为全新 `/asset_*` 专用接口，引入页面级资产标签管理弹窗。
- **威胁雷达精准时区计算**：GitHub 威胁情报雷达改用 UTC 24 小时绝对时间差与原子级 upsert 更新，防止漏报与重复跨线程推送。

### 🛠️ 性能与重构
- **数据库批量写入与联合唯一索引**：全面引入 `bulk_write` 与 `safe_insert_asset`，为 13 张核心资产表建立联合唯一索引，彻底杜绝极端高并发下的数据重复落库。
- **StatelessSession 网络连接池**：重构底层网络请求池，强制自适应连接回收，新增 10MB 响应体截断保护机制，规避大文件下载引发的内存泄漏。
- **浏览器微服务自愈机制**：重构 Puppeteer 截图服务，增加滚动重启（Rolling Restart）与防僵尸进程清理逻辑，修复高并发扫描下的 HTTP 503 异常。

---

## [v1.1.6] - 2026-07-28

### 🚀 新增功能
- **Puppeteer 架构微服务化**：剥离 Puppeteer 为独立常驻微服务（`arl-puppeteer`），彻底解决浏览器扫描时的 CPU 资源竞争瓶颈。
- **任务级联删除与资产关联**：任务列表删除操作新增二次确认弹窗，支持勾选“同时删除关联资产数据”。
- **爬虫反检测对抗**：Node.js 爬虫服务新增 `navigator.webdriver` 抹除和真实 UA 伪装，绕过基础 WAF 防爬策略。
- **站点截图即时预览**：资产分组详情页与资产检索页支持站点截图快速弹窗预览。

### 🛠️ 性能与重构
- **AC 自动机指纹加速**：引入 `pyahocorasick` 算法加速指纹特征匹配，并通过 `lru_cache` 预计算大幅提升正则与敏感文件泄漏比对性能。
- **启动检查动态就绪**：`start-prod.sh` 引入后端 API 动态就绪轮询探针，替代固定 sleep 时长。

---

## [v1.1.5] - 2026-07-25

### 🛠️ 性能与重构
- **部署脚本浅克隆优化**：优化生产环境自动化部署对 GitHub 浅拉取（Shallow Clone）源码的兼容性与构建容错。

---

## [v1.1.4] - 2026-07-24

### 🚀 新增功能与加固
- **一键部署底层健壮性重构**：新增并发防冲突锁、配置文件原子级写入与网络断连自动重试；自动清理幽灵容器与磁盘虚悬镜像；延长平滑停机时间至 60 秒。
- **Host 碰撞策略修复**：补齐策略中缺失的 Host 碰撞配置，确保后台任务联动与正常下发。
- **样式细节修复**：修复全局背景样式，解决长页面滚动到底部时出现的白边与背景闪烁问题。

---

## [v1.1.3] - 2026-07-20

### 🚀 新增功能
- **AI 原生架构 (MCP Server)**：首次引入独立 `mcp-server` 模块，原生支持 Model Context Protocol，赋能外部 AI 大模型穿透调度资产系统。
- **前端动态主题引擎**：引入 `theme.js` 动态引擎，支持自定义背景与全局动态配色，样式系统全面解耦。
- **实时更新终端渲染**：后台引入基于 PTY 的虚拟终端技术，解析 Docker 原生进度条与 ANSI 字符，在前端日志区实现 MB 级平滑下载进度呈现。

### 🛡️ 安全与配置
- **生产级安全网关加固**：首次部署自动生成强口令 `.htpasswd` (Basic Auth)，有效防御公网被动探测。

---

## [v1.1.2] - 2026-07-16

### 🛠️ 性能与重构
- **系统一键升级与 Nuclei 升级**：集成 Nuclei 最新扫描引擎，完善前端系统更新触发机制与 Auth 拦截器修复。

---

## [v1.1.1] - 2026-07-14

### 🚀 新增功能
- **PoC 深度管理体系**：支持在线查看、编辑与创建自定义 PoC（`ARLPoCCreate` / `ARLPoCSource`），并在删除时级联清理策略关联与本地插件文件。
- **字典可视化管理**：新增弱口令字典列表查询、内容预览、关键词检索与批量追加/删除功能。
- **IP 范围精准调度**：资产范围（Scope）与任务调度全面严格区分 `Domain` 与 `IP` 类型的校验规则。

---

## [v1.1.0] - 2026-07-12

### 🚀 新增功能
- **GitHub 威胁情报雷达**：引入 CVE 漏洞雷达、安全武器库追踪与黑客动态监测模块，支持按需启用与定时扫描。
- **告警推送生态拓展**：新增 Telegram 机器人推送支持，重构威胁告警推送链路。
- **HTTP/2 落地**：前端 Nginx 生产环境全面启用 HTTP/2 多路复用。

---

## [v1.0.8] - 2026-07-10

### 🚀 新增功能
- **PoC 批量导入**：支持 `.yml`、`.yaml`、`.py` 格式漏洞验证脚本批量上传、防目录穿越校验与数据库自动同步。
- **任务并发热扩缩容**：系统配置更新并发数时通过 Celery 控制广播（`pool_grow`/`pool_shrink`）实现即时热生效。

---

## [v1.0.6] - 2026-07-09

### 🚀 新增功能与优化
- **指纹库大幅扩充**：引入 1.6 万条增强指纹特征，优化指纹穿透查询逻辑。
- **Docker 镜像极速瘦身**：全面替换为 Headless Chromium，镜像减重 700MB+ 并接入阿里云源。

---

## [v1.0.0] - 2026-07-09

### 🎉 初始开源发布
- ARL-Next 全新资产侦察与漏洞治理平台初始版本正式开源发布。
